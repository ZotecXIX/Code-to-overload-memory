# Code-to-overload-memory
This is an executable file (made with python) that overloads a systems RAM.
Also this includes a program that writes charaters to a file and one that overloads a systems cpu.
